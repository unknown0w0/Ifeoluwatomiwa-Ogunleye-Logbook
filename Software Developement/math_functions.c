#include <stdio.h>
#include <math.h>

int main()
{
    float x = 9.4;

    x = round(x);
    //x = ceil(x);
    //x = floor(x);
    //x = pow(x, 4);
    //x = sqrt(x);
    //x = log(x);
    //x = sin(x);
    //x = abs(x);

    printf("%f", x);

}